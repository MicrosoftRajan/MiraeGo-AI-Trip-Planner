# Clerk (required)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend URL for production builds.
# Optional if vercel.json rewrites /api → Render (preferred for Vercel).
# Use this for direct browser → API calls (CORS already open on the backend).
# Example: https://miraego-ai-trip-planner.onrender.com
VITE_API_BASE_URL=
